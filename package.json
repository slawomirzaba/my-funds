{
  "name": "my-funds",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many -t build",
    "lint": "nx run-many -t lint",
    "type-check": "nx run-many -t type-check",
    "test": "nx run-many -t test",
    "prettier": "prettier \"{apps,packages}/**/*.{ts,tsx,js,json}\"",
    "format:check": "yarn prettier --check",
    "format:write": "yarn prettier --write",
    "dev": "nx run-many -t dev"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nx/js": "19.0.4",
    "nx": "19.0.4",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
